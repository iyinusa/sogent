# agents/__init__.py

from .agents import handle_message, sogent_agent, product_agent, greeting_agent, root_agent
from .router import router

# This file marks the agents directory as a Python package.
